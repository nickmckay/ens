% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propagate.R
\name{propagateUncertainty}
\alias{propagateUncertainty}
\title{Propagate uncertainty across an ensemble through any analysis function}
\usage{
propagateUncertainty(
  time,
  vals,
  changeFun,
  simulate.time.uncertainty = TRUE,
  simulate.paleo.uncertainty = TRUE,
  n.ens = 100,
  bam.model = list(ns = n.ens, name = "bernoulli", param = 0.05),
  paleo.uncertainty = sd(vals, na.rm = TRUE)/2,
  paleo.ar1 = sqrt(0.5),
  paleo.arima.order = c(1, 0, 0),
  summarize = FALSE,
  seed = round(sum(time, na.rm = TRUE)),
  progress = TRUE,
  ...
)
}
\arguments{
\item{time}{a time vector, or matrix of time ensemble members (ensembles in columns)}

\item{vals}{a values vector, or matrix of values ensemble members (ensembles in columns)}

\item{changeFun}{the analysis function to propagate across the ensemble. Must accept time and vals as its first two arguments and return a one-row data.frame/tibble.}

\item{simulate.time.uncertainty}{TRUE or FALSE. If an ensemble is not included, do you want to simulate time ensembles (default = TRUE)}

\item{simulate.paleo.uncertainty}{TRUE or FALSE. If an ensemble is not included, do you want to simulate paleo ensembles (default = TRUE)}

\item{n.ens}{How many ensembles to use for error propagation? (default = 100)}

\item{bam.model}{BAM Model parameters to use if simulating time uncertainty (default = list(ns = n.ens, name = "bernoulli", param = 0.05))}

\item{paleo.uncertainty}{Uncertainty to use if modelling uncertainty for paleo values. (default = sd(vals,na.rm = TRUE)/2)}

\item{paleo.ar1}{Autocorrelation coefficient to use for modelling uncertainty on paleoData, what fraction of the uncertainties are autocorrelated? (default = sqrt(0.5); or 50\% autocorrelated uncertainty)}

\item{paleo.arima.order}{Order to use for ARIMA model used in modelling uncertainty on paleoData (default = c(1,0,0))}

\item{summarize}{How to summarize the per-ensemble results. FALSE (default) returns every ensemble member. Alternatively, supply a function function(propagated) -> data.frame that collapses the propagated tibble into a summary; this replaces the previous hard-coded, analysis-specific summary so the engine stays generic across analyses.}

\item{seed}{set a seed for reproducibility}

\item{progress}{show a progress bar?}

\item{...}{arguments to pass to changeFun. Vectors longer than 1 are treated as parameter ensembles and sampled across the n.ens members.}
}
\value{
a tibble of propagated results (one row per ensemble member, unless summarized)
}
\description{
Generic engine for ensemble uncertainty propagation. Given a
time vector (or ensemble) and a values vector (or ensemble), and a
changeFun that operates on a single (time, vals) pair, propagateUncertainty
maps changeFun across n.ens ensemble members, simulating time and/or
value uncertainty when an ensemble is not supplied. Any analysis expressed as
changeFun(time, vals, ...) -> one-row data.frame/tibble automatically gains
ensemble uncertainty propagation.
}
\seealso{
Other ensemble: 
\code{\link{kdePval}()},
\code{\link{testNullHypothesis}()}
}
\concept{ensemble}
